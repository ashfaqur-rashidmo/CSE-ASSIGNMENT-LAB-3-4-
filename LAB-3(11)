#include <stdio.h>

int main() {
    int x;
    scanf("%d", &x);


    for (int i = 2; i < x; i++) {
        if (x % i == 0) {
            printf("NOt A Prime Number");
            return 0;
        }
    }

    printf("Is a Prime Number ");
    return 0;
}
